# 7/11 統合告知ポスト 設計仕様（作業再開用）

状態: 文言確定・資料反映済み。画像は①②完成、**③投票情報が未実装**。次セッションはこのファイルから再開。

## ポスト概要

- 投稿日: 7/11、運営アカウント1名が長文ポスト（青バッジ）。ライバーはいいね・リポスト・引用で支援
- 本文: 資料 `public/materials/index.html` の「統合告知（運営長文ポスト）」カード（7/11）に確定文言掲載済み
- 貫通BONUS告知は**このポストに含めない**。別日に単独投稿（画像は既存 `sns-7-11.png`、日程未定）

## 添付画像 3枚構成（この順で添付）

1. **キービジュアル**: `public/assets/promo/sns-6-13.png` — 既存をそのまま。タイムラインの引き
2. **全メンバー**: `public/assets/promo/sns-unified-hero.png` — 完成済み。7/1デザイン完全維持（FOURTH DROP / FULL ROSTER & TIME TABLE / 全メンバー・スケジュール解禁 / クリップ推奨 / 19:45・20:00・22:15）、唯一の差分は右上日付マーク=07.11 SAT。**これ以上一切変更しない**（完成デザインへの要素追加・文言変更は全部NGを食らった経緯あり）
3. **1票の価値（新規・未実装）**: 下記仕様で作成

### 画像は3枚で確定（4枚は多すぎNG）
- 6/27 投票ルール画像は**添付しない**。投票の時間（20:45/22:15）はポスト本文テキストで足りる。これを足すと4枚になり「多すぎ」になる。
- 最初に作った `sns-unified-vote.png`（HOW TO WIN / 3ステップ）は 6/27 とほぼ同内容の重複でNG。破棄（scene-vote ごと削除）。
- どの既存画像にも無い唯一の欠落 = **1票の金銭的価値**。画像に残す投票ネタはこれ1つだけ。

## 画像③「1票の価値」実装仕様

- 生成: `promo/sns-unified.html` に scene `value` を新設 → `promo/shot-sns-unified.mjs` の shots に `["value", "public/assets/promo/sns-unified-value.png"]` を追加 → `node promo/shot-sns-unified.mjs`
- 実装は **codex に指示**（プロンプトはファイルに書いて `codex exec --sandbox workspace-write - < prompt.txt`。引数渡し禁止）
- キャンバス: 1600×900、シリーズ共通の poster 背景・brand ロゴ・date-mark（**07.11 SAT**）・team-strip
- レイアウトは**新規**（投稿済み画像のレイアウト流用禁止。6/27 の投票カーブグラフは使わない＝重複回避）
- 伝える核心（装飾絵文字禁止・**「審査員なし」は書かない**＝ColorSingで審査員は元々いないので素人コピー）:
  - 主役の数字: **1票 = 最大 約3,700円分（ギフト換算）／ 100票 = 37万円分**
  - メッセージ: **課金力ではなく、応援の数が勝敗を分ける**（＝リスナー投票がギフトより重い、これが唯一の売り）
  - 後押し: ギフトが投げられなくても気負わず、1票で主役になれる／1人1票・無料
  - 導線: battle-fes.pages.dev
- 完成したらレンダリング画像を**ユーザーに見せて承認を得てから**資料カード反映・デプロイ（勝手に進めない）

## 添付画像セット（3枚で確定）
①キービジュアル `sns-6-13.png` ／ ②全メンバー `sns-unified-hero.png` ／ ③1票の価値 `sns-unified-value.png`（新規）

## 残作業チェックリスト

- [ ] 旧 `sns-unified-vote.png` と scene-vote を破棄
- [ ] 画像③「1票の価値」を codex で実装 → レンダリング → ユーザー承認
- [ ] 資料カードの画像欄を3枚構成に更新（①6-13 ②hero ③value）※現在は hero + BONUS の2枚のまま
- [ ] 資料の 7/17（前日）・7/18（当日）カードの扱いと、貫通BONUS単独投稿の日程をユーザーと決める
- [ ] デプロイ（`powershell -ExecutionPolicy Bypass -File .\scripts\deploy-safe.ps1`）と本番確認
- [ ] PROGRESS.md 更新
